'use strict';

function Footer() {
    return (
        <footer>
            <p></p>
        </footer>
    );
}

ReactDOM.createRoot(document.querySelector("footer"))?.render(React.createElement(Footer));
